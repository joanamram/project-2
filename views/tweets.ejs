<h3>Tweets</h3>

<section>
  <% tweets.forEach(function(tweet) { %>
    <img width="300" src="<%= tweet.picPath %>"/>
    <p><%= tweet.content %></p>
    <p>Favorites: <%= tweet.favorites %></p>
  <% })%>
</section>
